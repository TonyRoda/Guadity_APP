package com.example.guardity_app.network
/*
import retrofit2.Call
import retrofit2.http.*

data class Equipo(
    val id: Int,
    val codEquipo: String,
    val num: String,
    val loId: Int
)

interface ApiService {

    @GET("Equipos")
    fun listarEquipos(@Query("id") id: Int): Call<List<Equipo>>

    @POST("Equipos")
    fun registrarEquipo(@Body equipo: Equipo): Call<String>

    @DELETE("Equipos/{id}")
    fun desvincularEquipo(@Path("id") id: Int): Call<String>

    // EjemploController
    @GET("Ejemplo")
    fun saludar(@Query("id") id: Int): Call<String>

    // Aquí deberías agregar login/register usuarios cuando los tengas en tu API
    @POST("login")
    fun login(@Body usuario: Usuario): Call<ResponseBody>

    @POST("registro")
    fun register(@Body usuario: Usuario): Call<ResponseBody>
}*/
